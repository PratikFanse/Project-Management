import cookie from 'js-cookie';
import * as React from 'react';
import PublicRoutes from '../Public/Public.routes';
export default function AppRoutes(props){
    const getRoutes = ()=>"" 
    // cookie.get('access_token')? false:<PublicRoutes/>
    return(
        getRoutes()
    )
}